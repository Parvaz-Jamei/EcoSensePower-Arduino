# Measurement Template

Use `extras/templates/current-profile-template.csv` to record owner-side current evidence.

Required columns:

`board,profile,mode,current_uA,voltage_V,duration_ms,confidence,instrument,date,notes`

Never submit unverified current numbers as `MEASURED`.
